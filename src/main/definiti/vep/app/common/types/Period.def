package vep.app.common.types

import vep.app.common.verifications.dateIsBeforeOrEqual

type Period {
  start: Date
  end: Date

  verify {
    "The start of the period should be before the end"
    (period) => {
      dateIsBeforeOrEqual(period.start, period.end)
    }
  }
}